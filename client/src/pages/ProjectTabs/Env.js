import React from 'react'
import Environment from '../../components/Env';

const Env = () => {
    return (
        <div style={{
            display:"flex",
            flex:1,
            alignItems:"center",
            justifyContent:"center"
        }}>
            <Environment />
        </div>
    )
}

export default Env
